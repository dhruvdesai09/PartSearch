# Public API base URL (no trailing slash). Set in Vercel / local `.env` as VITE_API_BASE.
# Local backend default if unset: http://localhost:8000
VITE_API_BASE=http://localhost:8000
VITE_ADMIN_MODE=false
